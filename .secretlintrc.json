{
  "//": "COMMITTED config — generic rules only, safe to be public. Never add real personal identifiers (email/name/home path) here: this file ships in the repo, so embedding them leaks the very PII it guards. Put your personal patterns in the GITIGNORED .secretlintrc.local.json (opal merges it at scan time).",
  "rules": [
    {
      "id": "@secretlint/secretlint-rule-preset-recommend"
    },
    {
      "id": "@secretlint/secretlint-rule-pattern",
      "options": {
        "patterns": [
          {
            "name": "Generic Anthropic key",
            "pattern": "/sk-ant-[A-Za-z0-9_-]{20,}/"
          },
          {
            "name": "Private key block",
            "pattern": "/-----BEGIN[A-Z ]*PRIVATE KEY-----/"
          }
        ]
      }
    }
  ]
}
